---
/// Layouts
import MainLayout from '@layouts/main.astro'

/// Sections
import SalutePageSection from '@components/islands/salute.astro'
import AboutPageSection from '@components/islands/about.astro'
import ProjectsPageSection from '@components/islands/projects.astro'
import ContactPageSection from '@components/islands/contact.astro'

/// i18N Imports
import { getHomeDataInternational } from "@i18n/index.ts"
const { currentLocale } = Astro
const i18n = getHomeDataInternational({ currentLocale })

---
<MainLayout title={i18n.TITLE}>
		<SalutePageSection />
		<AboutPageSection />
		<ProjectsPageSection />
		<ContactPageSection />
</MainLayout>